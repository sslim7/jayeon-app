/**
 * 서비스 워커 등록 — **네이티브 스텁이다.**
 *
 * 파일이 둘로 갈리는 규칙과 그 이유는 `native-bridge.ts` 머리말과 같다. 네이티브 앱에는
 * 등록할 서비스 워커도, 그것을 돌릴 브라우저도 없다.
 */

export function registerServiceWorker(): void {
  // 네이티브다 — 등록할 것이 없다.
}
