/**
 * 껍데기 웹뷰 — **웹 스텁이다. 늘 `null` 을 그린다.**
 *
 * 웹은 껍데기 **안**이 아니라 껍데기가 여는 대상 그 자체다. 브라우저에서 도는 이 번들이
 * 자기 자신을 웹뷰로 한 번 더 여는 일은 있을 수 없으므로 그릴 것이 없다.
 *
 * **그래도 이 파일이 필요하다.** 없으면 번들러가 웹 빌드에서도 `web-shell.tsx` 를 고르고,
 * 그 파일이 정적으로 import 하는 `react-native-webview` 가 웹 번들에 통째로 끌려 들어온다.
 * 그 패키지에는 웹 구현이 없어서(확인: `react-native-webview@13` 의 `lib/` 에 `.web` 파일이
 * 없다) 그리는 순간 「이 플랫폼을 지원하지 않는다」는 붉은 글자가 나온다.
 *
 * 플랫폼 확장자 규칙과 그 이유는 `lib/native-bridge.ts` 머리말에 적어 뒀다.
 */
export function WebShell() {
  return null;
}
