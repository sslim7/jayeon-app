/**
 * 프리티어 설정.
 *
 * **이 저장소의 코드는 프리티어로 맞춰져 있지 않다.** 모양은 사람이 손으로 잡는다.
 * eslint 설정(`eslint-config-expo`)에도 따옴표·줄 길이 같은 모양 규칙이 없어서,
 * 이 파일이 없으면 모양을 정하는 곳이 아무 데도 없게 된다.
 *
 * 그런데도 이 파일을 두는 이유는, 설정이 **없을 때** 프리티어가 자기 기본값(쌍따옴표·80칸)으로
 * 도는 것이 훨씬 나쁘기 때문이다. 형제 저장소(birdieup-app)에서 실제로 그 일이 있었다 —
 * 한 파일이 통째로 다른 모양이 돼 이웃 파일들과 갈렸다. 여기 값을 적어 두면 누가 어디서
 * 돌리든 적어도 우리 규칙(홑따옴표·100칸)을 따른다.
 *
 * **`prettier --write .` 로 저장소 전체를 돌리지 마라.** 손으로 잡아 온 줄바꿈까지 통째로
 * 다시 접혀 리뷰할 수 없는 커밋이 된다. 필요한 파일 하나에만 쓴다.
 */
module.exports = {
  singleQuote: true,
  printWidth: 100,
};
